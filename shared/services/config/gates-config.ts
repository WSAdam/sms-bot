// MIGRATION SHIM → gates-config moved to src/core/business/gates-config during
// the shape-checker migration. Kept so existing importers keep working untouched.
export * from "@core/business/gates-config/mod.ts";
