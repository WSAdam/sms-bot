// MIGRATION SHIM → moved to src/core/dto/orchestrator.ts during the shape-checker
// migration. Kept so existing @shared/types/orchestrator.ts importers keep working.
export * from "@core/dto/orchestrator.ts";
